name: lilafestivalapp
description: A festival app.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  flutter_hooks: ^0.10.0
  hooks_riverpod: ^0.3.0
  i18n_extension: ^1.4.1
  dime: ^0.4.0
  immortal: ^2.1.1
  url_launcher: ^5.4.11
  package_info: ^0.4.1
  path_provider: ^1.6.11
  http: ^0.12.1
  cached_network_image: ^2.2.0+1
  weather: ^1.1.1
  dcache: ^0.3.0
  flutter_config: ^1.0.7 # TODO(SF) BUILD can this be a dev dependency?
  webview_flutter: ^0.3.22+1
  flutter_local_notifications: ^1.4.4+2
  # flutter_launcher_icons: ^0.7.2+1

dev_dependencies:
  flutter_test:
    sdk: flutter
  effective_dart: ^1.2.3
  mockito: ^4.1.1

flutter:
  uses-material-design: true

  assets:
     - assets/initial_schedule.json
     - assets/bands.json
     - assets/spirit/
    #  - assets/party.san/

  fonts:
    - family: No Continue
      fonts:
        - asset: assets/spirit/no_continue.ttf
    # - family: Pirata One
    #   fonts:
    #     - asset: assets/party.san/PirataOne-Regular.ttf

# flutter_icons:
#   image_path: "assets/icon.png"
#   android: true
#   adaptive_icon_background: "#000000"
#   adaptive_icon_foreground: "assets/icon_adaptive.png"
